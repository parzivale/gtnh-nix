[project]
name = "gtnh-nix"
version = "0.1.0"
description = "GTNH Nix configuration tools"
requires-python = ">=3.11"
dependencies = ["pyhocon>=0.3.60"]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["scripts/parsers/tests"]
pythonpath = ["."]
